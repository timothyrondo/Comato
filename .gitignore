node_modules/
dist/
build/
*.log

# env / secrets
.env
.env.*
!.env.example
# apps/web public build config — no secrets, must ship with the Vercel build.
!apps/web/.env.production

# foundry
packages/contracts/out/
packages/contracts/cache/
packages/contracts/broadcast/**/dry-run/

# vite
apps/web/dist/
.DS_Store

# installed skill tooling (re-derivable from skills-lock.json) + local Claude config
.agents/
.claude/

# runtime state (rate-limiter persistence, quote store)
.comato/
